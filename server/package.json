{
  "name": "@makecode-sprite-generator/server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "source .venv/bin/activate && export PYTHONPATH=.:$PYTHONPATH && python -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "start": "source .venv/bin/activate && python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "freeze": "source .venv/bin/activate && pip freeze > requirements.txt",
    "test": "source .venv/bin/activate && pytest",
    "setup": "python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt"
  }
}
