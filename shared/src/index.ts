// @makespritecode/shared — the single source of truth for the client/server
// wire contract: Zod schemas (runtime validation on the server) + the inferred
// TypeScript types (consumed by the client). Raw TS, no build step.
export * from "./enums";
export * from "./palette";
export * from "./wire";
