export enum EditorTools {
  Pencil = "pencil",
  Eraser = "eraser",
  Fill = "fill",
  Line = "line",
  Rectangle = "rectangle",
  Circle = "circle",
  Select = "select",
  Pan = "pan",
}

export type EditorToolType =
  | "pencil"
  | "eraser"
  | "fill"
  | "line"
  | "rectangle"
  | "circle"
  | "select"
  | "pan";
